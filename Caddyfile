compostmodernism.org {
	reverse_proxy compostmodernism:3000

	# Larger than Caddy's default — image uploads can be multi-MB
	request_body {
		max_size 20MB
	}

	log {
		output stdout
		format console
	}
}

www.compostmodernism.org {
	redir https://compostmodernism.org{uri} permanent
}
